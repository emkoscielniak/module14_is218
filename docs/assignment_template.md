# Module 9 Submission Template

Use this template to compile your screenshots and brief captions into a Word document or PDF for submission.

1) Title Page
- Assignment: Module 9 — FastAPI + PostgreSQL + pgAdmin
- Your name, course, date

2) Screenshots (one per required SQL step)
- Table Creation: Screenshot of `create_tables.sql` execution showing success message.
- Insert Records: Screenshot of `insert_data.sql` execution showing rows inserted.
- Query Data: Screenshot(s) of `queries.sql` showing results and joins.
- Update Record: Screenshot of `update_delete.sql` after running UPDATE.
- Delete Record: Screenshot of `update_delete.sql` after running DELETE.

For each screenshot include a one-line caption describing what the screenshot shows.

3) Reflection
- 1–2 paragraphs describing:
  - Key steps you completed
  - Any issues encountered (e.g., connection problems, permission errors)
  - What you learned about foreign keys and one-to-many relationships

4) Optional: Attach development logs or terminal output showing `docker-compose up --build` started successfully.
